# Stock Pattern Scanner using Deep Learning & Streamlit

This project fetches financial data, converts candlestick charts into raw images, trains a Convolutional Neural Network (CNN) to recognize patterns, and displays real-time inferences on a Streamlit dashboard.

## Local Setup
1. **Clone the repo and install dependencies:**
   ```bash
   pip install -r requirements.txt
